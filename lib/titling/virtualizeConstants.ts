/** Minimum row count before window virtualization activates on /titling. */
export const TITLING_VIRTUALIZE_THRESHOLD = 20

/** Initial row height guess for titling cards (beneficiary rows vary). */
export const TITLING_CARD_ESTIMATED_HEIGHT = 320

/** Matches Tailwind `space-y-4` gap between cards. */
export const TITLING_CARD_LIST_GAP = 16
