# Deprecated — use the env file that matches your target:
#   .env.test.local.example      → copy to .env.test.local       (npm run test:e2e)
#   .env.test.staging.example    → copy to .env.test.staging     (npm run test:e2e:staging)
#   .env.test.production.example → copy to .env.test.production  (npm run test:e2e:prod:smoke)
#
# Base URLs live in scripts/testEnv.ts (TEST_ENV=local|staging|production).
